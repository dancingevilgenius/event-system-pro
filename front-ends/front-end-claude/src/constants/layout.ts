/** Max width for main content blocks: messages, buttons, fields, dropdowns. */
export const CONTENT_MAX_WIDTH = 360;

/** Total number of couples judged per session. */
export const COUPLE_COUNT = 20;

/** Valid bib number range. */
export const BIB_MIN = 1;
export const BIB_MAX = 999;

/** Raw score bounds. */
export const SCORE_MIN = 0;
export const SCORE_MAX = 99.99;

/** Duplicate-resolution score nudge. */
export const DUPLICATE_NUDGE = 0.1;

/** Assign Random Scores bounds. */
export const RANDOM_SCORE_MIN = 30.0;
export const RANDOM_SCORE_MAX = 99.9;
